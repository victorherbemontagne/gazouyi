import { Ci18nImage } from './ci18n-image';

/**
 * Step in "First steps with babycloud" in i18n custom files
 * @interface C18nStep
 */
export interface Ci18nStep {
    IMG: Ci18nImage;
    NAME: string;
    DESCRIPTION: string;
}
