import { Ci18nLink } from './ci18n-link';

/**
 * A contact column for the footer in i18n custom files
 * @interface Ci18nFooterCol
 */
export interface Ci18nFooterCol {
    TITLE: string;
    LINKS: Ci18nLink[];
}
